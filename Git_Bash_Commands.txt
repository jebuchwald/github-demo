pwd
  - shows current working directory

mkdir
  - creates a new folder

cd
  - changes directory

echo "Text here" >> filename.txt
  - creates a file with text specified

ls
  - lists contents of current directory

rm  [-f | --force] {[-i | --interactive[=always]] | [-I | --interactive=once] |
[--interactive=never]} [--one-file-system] [--no-preserve-root |
--preserve-root] [-r | -R | --recursive] [-d | --dir] [-v | --verbose] [file or directory]
  - removes file or directory

mv [filename][new file path]
  -move or rename a file




git version
  - shows version

git config --global [user.name, user.email] "string"
  - specify global variables

git config --global --list
  - shows global variables

git status
  - shows status of files in working, staging and master

git add file.ext
  - adds specified file to staging area

git commmit -m "note for the commit"
  - Commits what in staging with specified comment

git push origin master
  - Commits local repository to master repository
